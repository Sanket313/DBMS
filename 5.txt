CREATE TABLE Sales (
    product_id INT,
    amount DECIMAL(10,2),
    region VARCHAR(50)
);

INSERT INTO Sales VALUES
(101, 2000.00, 'North'),
(102, 3500.00, 'South'),
(103, 4000.00, 'East'),
(104, 2500.00, 'West'),
(105, 3000.00, 'North'),
(106, 4500.00, 'South'),
(107, 1500.00, 'East'),
(108, 5000.00, 'West'),
(109, 2200.00, 'North'),
(110, 1800.00, 'South');

SELECT region, SUM(amount) AS total_sales
FROM Sales
GROUP BY region;

SELECT AVG(amount) AS average_sales
FROM Sales;

SELECT region, SUM(amount) AS total_sales
FROM Sales
GROUP BY region
HAVING SUM(amo
